server {
    listen 80;

    server_name ~^(?<subdomain>.+)\.localhost$;

    root /var/www/$subdomain;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}server {
    listen 80;

    server_name ~^(?<subdomain>.+)\.localhost$;

    root /var/www/$subdomain;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}